{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "APP_ID",
      "description": "Application ID (from the project's deployment platform). Not used in dev mode but required for production.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    },
    {
      "name": "APP_SECRET",
      "description": "Application secret used for JWT signing in production. Not used in dev mode.",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
